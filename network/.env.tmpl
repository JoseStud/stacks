# Network — Vaultwarden + Pi-hole + Orbital Sync
# Sources: /infrastructure, /stacks/network

{{- with secret "/infrastructure" }}
BASE_DOMAIN={{ .BASE_DOMAIN }}
TZ={{ .TZ }}
{{- end }}

{{- with secret "/stacks/network" }}
VW_DB_PASS={{ .VW_DB_PASS }}
VW_ADMIN_TOKEN={{ .VW_ADMIN_TOKEN }}
PIHOLE_PASSWORD={{ .PIHOLE_PASSWORD }}
PIHOLE_UPSTREAMS=1.1.1.1;8.8.8.8
{{- end }}
